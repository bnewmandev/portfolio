{
  "name": "server",
  "version": "1.0.0",
  "main": "src/server.ts",
  "author": "Ben Newman",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.2",
    "express": "^4.17.3",
    "ts-node-dev": "^1.1.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "nodemon": "^2.0.15",
    "prettier": "^2.6.0",
    "tslint": "^6.1.3",
    "typescript": "^4.6.2"
  },
  "scripts": {
    "start": "node ./dist/server.js",
    "prestart": "tsc",
    "test": "ts-node-dev --respawn --pretty --transpile-only ./src/server.ts",
    "pretest": "tslint -c tslint.json -p tsconfig.json --fix"
  }
}
